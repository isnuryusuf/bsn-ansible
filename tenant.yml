---

   - name: GATHER FACTS FROM BIG SWITCH CONTROLLERS
     hosts: bcf
     connection: local

     tasks:
       - name: CREATE TENANT
         #no_log: True
         bcf_methods2: controller={{ controller_ip }} username={{ un }} password={{ pwd }} tenant_name="test-tenant4"

       - name: CREATE SEGMENT
         #no_log: True
         bcf_methods2: controller={{ controller_ip }} username={{ un }} password={{ pwd }} tenant_name="test-tenant4" segment_name="test-segment"

         #- name: DUMP FACTS TO TERMINAL
         #debug: var=bsnbcf

